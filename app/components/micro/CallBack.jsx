import React from 'react'

import styles from '@/app/css/header.module.css'

export const CallBack = ({}) => {
  return(
  <button className = {styles.buttonCallBack}><a href = "/#mainForm">Заказать звонок</a></button>
  )
}
