import {createSlice} from '@reduxjs/toolkit';

const inititalState = [];

const searchSlice = createSlice({
    name: 'search',
    inititalState,
    reducer: {
        add: (state, action) => {
        },
    }
})